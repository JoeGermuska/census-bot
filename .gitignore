# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — NEVER commit this
.env.local
.env.development.local
.env.test.local
.env.production.local

# raw ACS source files — regeneratable via `npm run fetch:acs-docs` and
# `npm run fetch:acs-tables`. Only the indexed JSON files (acs-data/index.json,
# acs-data/tables-index.json) are checked in.
acs-data/raw/

# vercel
.vercel
.claude/worktrees/
.claude/settings.local.json

# Local-only IPUMS testing sandbox — never committed.
# Contains IPUMS API key in .env and downloaded NHGIS extracts.
ipums-sandbox/

# Local-only experimental historical-data chat route. Lives on disk for the
# dev server to pick up; never committed, never deployed. To promote, copy
# the relevant pieces into the real chat.js and remove these gitignore lines.
pages/chat-experimental.js
pages/api/chat-experimental.js
lib/historicalCensus.js
lib/historicalCensus.json
