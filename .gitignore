# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — NEVER commit this
.env.local
.env.development.local
.env.test.local
.env.production.local

# raw ACS source files — regeneratable via `npm run fetch:acs-docs` and
# `npm run fetch:acs-tables`. Only the indexed JSON files (docs/index.json,
# docs/tables-index.json) are checked in.
docs/raw/

# vercel
.vercel
.claude/worktrees/
.claude/settings.local.json

# Local-only metric-graph exploration page (kept on disk, not shipped).
# Regenerate the layout via `node scripts/build-graph-layout.mjs` whenever
# the underlying lib/acsAlternatives.json data changes.
pages/test/metric-graph.js
pages/api/graph-layout.js
scripts/build-graph-layout.mjs
scripts/derive-acs-alternatives.mjs
lib/graphLayout.json
lib/acsAlternatives.json
